# Database credentials
# Set SPRING_DATASOURCE_PASSWORD before running the application outside of Docker Compose.
# When using docker-compose, the password is injected via the SPRING_DATASOURCE_PASSWORD environment variable.
SPRING_DATASOURCE_PASSWORD=
